<style type="text/tailwindcss">
  /* ================================================
     Buttons
     ================================================ */

  .btn-primary {
    @apply bg-indigo-600 dark:bg-indigo-500 text-white px-4 py-2 rounded-md text-sm font-medium hover:bg-indigo-700 dark:hover:bg-indigo-600;
  }

  .btn-primary-full {
    @apply relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-indigo-600 dark:bg-indigo-500 hover:bg-indigo-700 dark:hover:bg-indigo-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500;
  }

  .btn-primary-lg {
    @apply inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-indigo-600 dark:bg-indigo-500 hover:bg-indigo-700 dark:hover:bg-indigo-600;
  }

  /* ================================================
     Links & Navigation
     ================================================ */

  .link-primary {
    @apply font-medium text-indigo-600 dark:text-indigo-400 hover:text-indigo-500 dark:hover:text-indigo-300;
  }

  .link-nav {
    @apply text-gray-500 dark:text-gray-300 hover:text-gray-700 dark:hover:text-gray-100 inline-flex items-center px-3 py-2 text-sm font-medium border-b-2 border-transparent;
  }

  .link-nav-active {
    @apply text-gray-900 dark:text-gray-100 hover:text-gray-700 dark:hover:text-gray-100 inline-flex items-center px-3 py-2 text-sm font-medium border-b-2 border-indigo-500 dark:border-indigo-400;
  }

  /* ================================================
     Form Inputs
     ================================================ */

  .input-base {
    @apply appearance-none relative block w-full px-3 py-2 border border-gray-300 dark:border-gray-600 placeholder-gray-500 dark:placeholder-gray-400 text-gray-900 dark:text-gray-100 bg-white dark:bg-gray-700 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm;
  }

  .input-top {
    @apply input-base rounded-t-md;
  }

  .input-middle {
    @apply input-base;
  }

  .input-bottom {
    @apply input-base rounded-b-md;
  }

  /* ================================================
     Typography
     ================================================ */

  .text-primary {
    @apply text-gray-900 dark:text-gray-100;
  }

  .text-secondary {
    @apply text-gray-700 dark:text-gray-300;
  }

  .text-muted {
    @apply text-gray-600 dark:text-gray-400;
  }

  .text-label {
    @apply text-gray-500 dark:text-gray-400;
  }

  .text-brand {
    @apply text-indigo-600 dark:text-indigo-400;
  }

  .heading-xl {
    @apply text-4xl font-bold text-gray-900 dark:text-gray-100;
  }

  .heading-lg {
    @apply text-3xl font-bold text-gray-900 dark:text-gray-100;
  }

  .heading-md {
    @apply text-2xl font-semibold text-gray-900 dark:text-gray-100;
  }

  .heading-sm {
    @apply text-lg leading-6 font-medium text-gray-900 dark:text-gray-100;
  }

  .subtitle {
    @apply text-xl text-gray-600 dark:text-gray-400;
  }

  /* ================================================
     Layout & Containers
     ================================================ */

  .centered-container {
    @apply min-h-[calc(100vh-10rem)] flex items-center justify-center -mx-4 sm:-mx-6 lg:-mx-8 -my-6;
  }

  .form-wrapper {
    @apply max-w-md w-full space-y-8 px-4;
  }

  .page-heading {
    @apply mt-6 text-center text-3xl font-extrabold text-primary;
  }

  .nav-container {
    @apply bg-white dark:bg-gray-800 shadow-lg;
  }

  .card {
    @apply bg-white dark:bg-gray-700 shadow overflow-hidden sm:rounded-lg text-gray-900 dark:text-gray-100;
  }

  .card-sm {
    @apply bg-white dark:bg-gray-700 overflow-hidden shadow rounded-lg text-gray-900 dark:text-gray-100;
  }

  .card-padding {
    @apply px-4 py-5 sm:p-6;
  }

  /* ================================================
     Alerts & Messages
     ================================================ */

  .alert-success {
    @apply mb-4 bg-green-100 dark:bg-green-900 border border-green-400 dark:border-green-600 text-green-700 dark:text-green-200 px-4 py-3 rounded relative;
  }

  .alert-error {
    @apply mb-4 bg-red-100 dark:bg-red-900 border border-red-400 dark:border-red-600 text-red-700 dark:text-red-200 px-4 py-3 rounded relative;
  }
</style>

