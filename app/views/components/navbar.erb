<nav class="bg-white dark:bg-gray-800 shadow-lg">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center py-2">
      <div class="flex items-center flex-1">
        <div class="flex-shrink-0 flex items-center mr-8">
          <%= render 'components/link', locals: { href: '/', text: 'Tails 🦊', variant: 'brand' } %>
        </div>
        <div class="flex-1 -mb-px flex space-x-8">
          <% if @current_user %>
            <%= render 'components/link', locals: { href: '/', text: 'Dashboard', variant: 'nav', active: request.path == '/' } %>
          <% end %>
        </div>
      </div>
      <div class="flex items-center space-x-4">
        <% if @current_user %>
          <span class="text-gray-700 dark:text-gray-300 text-sm"><%= @current_user.email %></span>
          <%= render 'components/link', locals: { href: '/auth/logout', text: 'Logout', variant: 'button' } %>
        <% else %>
          <%= render 'components/link', locals: { href: '/auth/login', text: 'Login', variant: 'primary', css_class: 'text-sm' } %>
          <%= render 'components/link', locals: { href: '/auth/register', text: 'Register', variant: 'button' } %>
        <% end %>
      </div>
    </div>
  </div>
</nav>

