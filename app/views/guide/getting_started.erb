<section id="getting-started" data-tab-content="getting-started" class="mb-16">
  <div class="card max-w-4xl mx-auto">
    <div class="card-padding">
      <div class="space-y-8">
        
        <div>
          <h3 class="heading-md mb-4">Setup & Installation</h3>
          <div class="space-y-4">
            <div>
              <h4 class="heading-sm mb-2">Quick Start</h4>
              <pre class="bg-gray-100 dark:bg-gray-600 p-4 rounded-lg overflow-x-auto"><code>git clone &lt;your-repo&gt;
cd tails
bin/setup</code></pre>
              <p class="text-muted text-sm mt-2">The setup script will:</p>
              <ul class="text-muted text-sm list-disc list-inside ml-2 space-y-1">
                <li>Create <code class="text-xs bg-gray-100 dark:bg-gray-600 px-1 rounded">.env</code> with a secure session secret</li>
                <li>Install dependencies</li>
                <li>Run database migrations</li>
                <li>Seed demo data</li>
              </ul>
            </div>

            <div>
              <h4 class="heading-sm mb-2">Run Development Server</h4>
              <pre class="bg-gray-100 dark:bg-gray-600 p-4 rounded-lg overflow-x-auto"><code>bin/dev</code></pre>
              <p class="text-muted text-sm mt-2">This starts both the web server and Tailwind CSS watcher</p>
            </div>

            <div>
              <h4 class="heading-sm mb-2">Demo Credentials</h4>
              <pre class="bg-gray-100 dark:bg-gray-600 p-4 rounded-lg overflow-x-auto"><code>Email: demo@example.com
Password: password123</code></pre>
            </div>

            <div class="mt-6 p-4 bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-lg">
              <p class="text-sm text-secondary">
                <strong>Note:</strong> Environment variables are automatically configured by <code class="text-xs bg-gray-100 dark:bg-gray-600 px-1 rounded">bin/setup</code>. The session secret is generated using SecureRandom for security.
              </p>
            </div>
          </div>
        </div>

        <div>
          <h3 class="heading-md mb-4">Project Structure</h3>
          <pre class="bg-gray-100 dark:bg-gray-600 p-4 rounded-lg overflow-x-auto text-sm"><code>app/
  ├── app.rb              # Main Roda application
  ├── models/             # ActiveRecord models
  ├── routes/             # Route handlers (multi_route)
  ├── services/           # Business logic services
  ├── helpers/            # Helper modules
  └── views/
      ├── components/     # Reusable ERB components
      ├── layouts/        # Layout templates
      └── pages/          # Page templates

config/
  ├── environment.rb      # App initialization
  └── rack_attack.rb      # Rate limiting config

db/
  ├── migrate/            # Database migrations
  └── seeds.rb            # Seed data</code></pre>
        </div>

        <div>
          <h3 class="heading-md mb-4">Key Technologies</h3>
          <ul class="space-y-3">
            <%= render('components/feature_item', locals: { label: 'Roda', description: 'Fast, tree-based routing web framework' }) %>
            <%= render('components/feature_item', locals: { label: 'ActiveRecord', description: 'Database ORM with migrations' }) %>
            <%= render('components/feature_item', locals: { label: 'ERB', description: 'Embedded Ruby templates' }) %>
            <%= render('components/feature_item', locals: { label: 'Tailwind CSS', description: 'Utility-first CSS with custom components' }) %>
            <%= render('components/feature_item', locals: { label: 'Falcon', description: 'High-performance web server' }) %>
            <%= render('components/feature_item', locals: { label: 'BCrypt', description: 'Secure password hashing' }) %>
          </ul>
        </div>

      </div>
    </div>
  </div>
</section>

