#!/usr/bin/env bash

if [ ! -f .env ]; then
  echo "Error: .env file not found!"
  echo "Please run ./bin/setup first or copy env.example to .env"
  exit 1
fi

bundle exec rerun --clear -- falcon serve --bind http://localhost:1234

